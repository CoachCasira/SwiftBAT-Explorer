name: Verifica progetto Maven

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    name: Compilazione e test Java 17
    runs-on: ubuntu-latest
    steps:
      - name: Scarica il progetto
        uses: actions/checkout@v4
        with:
          ref: feature/analysis-population-1.3.0
          fetch-depth: 0

      - name: Configura Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Applica rifiniture layout
        run: python3 .github/scripts/apply_layout_refinements.py

      - name: Verifica Maven
        run: mvn --batch-mode --no-transfer-progress verify

      - name: Salva rifiniture layout
        if: success()
        shell: bash
        run: |
          git config user.name "ChatGPT"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/it/casiraghi/swiftbat/ui/PopulationPage.java \
                  src/main/java/it/casiraghi/swiftbat/ui/SkyMapPage.java \
                  src/main/resources/app.css
          if git diff --cached --quiet; then
            echo "Nessuna modifica da salvare"
          else
            git commit -m "Riorganizza filtri popolazione e mappa celeste"
            git push
          fi
