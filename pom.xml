<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>it.casiraghi</groupId>
    <artifactId>swiftbat-explorer</artifactId>
    <version>1.3.0</version>
    <name>SwiftBAT Explorer</name>
    <description>Applicazione JavaFX online per esplorazione, comprensione e confronto delle curve di luce e distribuzione celeste Swift/BAT GRB</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <javafx.version>17.0.19</javafx.version>
        <jsoup.version>1.22.2</jsoup.version>
        <nom-tam-fits.version>1.22.1</nom-tam-fits.version>
        <junit.version>5.13.4</junit.version>
        <poi.version>5.5.1</poi.version>
        <!-- Su JDK 24+ il profilo in fondo sostituisce questa opzione neutra. -->
        <javafx.unsafe.memory.option>-Dswiftbat.jdk.compatibility=true</javafx.unsafe.memory.option>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>gov.nasa.gsfc.heasarc</groupId>
            <artifactId>nom-tam-fits</artifactId>
            <version>${nom-tam-fits.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>${jsoup.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>17</release>
                    <encoding>UTF-8</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>it.casiraghi.swiftbat.Launcher</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.8.1</version>
                <executions>
                    <execution>
                        <id>copy-runtime-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeScope>runtime</includeScope>
                            <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>it.casiraghi.swiftbat.SwiftBatExplorerApp</mainClass>
                    <launcher>swiftbat-explorer</launcher>
                    <options>
                        <option>--enable-native-access=javafx.graphics,javafx.swing</option>
                        <option>-Dfile.encoding=UTF-8</option>
                        <option>${javafx.unsafe.memory.option}</option>
                    </options>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>jdk-24-plus</id>
            <activation>
                <jdk>[24,)</jdk>
            </activation>
            <properties>
                <!-- JavaFX 17 usa ancora internamente Unsafe; su JDK 24+ questa
                     opzione evita il warning senza modificare i dati o il rendering. -->
                <javafx.unsafe.memory.option>--sun-misc-unsafe-memory-access=allow</javafx.unsafe.memory.option>
            </properties>
        </profile>
    </profiles>
</project>
